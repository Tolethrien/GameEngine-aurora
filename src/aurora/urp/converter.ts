export default class GPUConverter {
  static convertPercentToPixels() {
    //convert percent to pixels on screen
  }
  static convertPixelsToPercent() {
    //convert pixel to percent
  }
  static convertToConstantPercent() {
    //converts value to constant 1% in width of screen
  }
}
